<h1>All Tags</h1>

<ul>
  <% @tags.each do |tag| %>
      <li class='tag_list'>
        <%= link_to tag.name, tag_path(tag) %>
        <% if logged_in? %>
            <%= link_to 'Delete', tag_path(tag), method: :delete %>
        <% end %>
      </li>
  <% end %>
</ul>